package javav.mvc.view;
//5

import javax.swing.*;

public class TotalComision extends JPanel {
    public TotalComision(){
        JPanel panel = new JPanel();
        JLabel label = new JLabel("Sarcina 5:");
        panel.add(label);

        add(panel);
        setVisible(true);
    }
}
